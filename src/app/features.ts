/**
 * 段階的に公開する機能のスイッチ。
 *
 * waveQuiz（ウェーブ後の確認テスト）：
 * Phase 1 では選択式で実装していたが、最終仕様はスマートフォンのキーボードから
 * アルファベット・ひらがなを直接入力する方式。Phase 1-C2 で直接入力方式へ
 * 差し替えたため true にしてある。
 * 受験そのものは任意で、スキップしても記録や進行で不利にならない。
 */
export const FEATURES = {
  waveQuiz: true,
} as const;
