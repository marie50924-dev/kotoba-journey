/**
 * 段階的に公開する機能のスイッチ。
 *
 * Phase 1 統合時点の判断：
 * - waveQuiz（ウェーブ後の確認テスト）は Phase 1 で選択式として実装したが、
 *   最終仕様はスマートフォンのキーボードからアルファベット・ひらがなを
 *   直接入力する方式に決定した。選択式のまま公開すると仕様と異なる遊び方が
 *   定着してしまうため、通常導線から外して false にしてある。
 *   画面・ドメイン・保存フィールドは Phase 1-C2 で直接入力テストへ
 *   差し替えるための境界として残す。削除はしない。
 */
export const FEATURES = {
  waveQuiz: false,
} as const;
